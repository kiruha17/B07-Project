<!-- fragment_top_page.xml -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Welcome message -->
    <TextView
        android:id="@+id/welcome_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome, User!"
        android:textSize="18sp"
        android:layout_marginBottom="20dp" />

    <!-- Temporary Annual Carbon Footprint Button -->

    <Button
        android:id="@+id/acf_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Annual Carbon Footprint" />

    <!-- Temporary Eco Hub Button -->

    <Button
        android:id="@+id/eco_hub_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Eco Hub" />

    <!-- Temporary Eco Gauge Button -->

    <Button
        android:id="@+id/eco_gauge_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Eco Gauge" />

    <!-- Logout button -->
    <Button
        android:id="@+id/logout_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Logout" />

    <!-- You can add more UI elements like user profile, statistics, etc. -->

</LinearLayout>
